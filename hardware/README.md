# Hardware

This folder hosts CAD drawings and mechanical design assets.

Move existing DWG files from the legacy `CAD-models` folder here (keeping originals until validated).

Proposed sub-folders:
- `cad/` raw CAD source (.dwg, .step)
- `renders/` exported images
- `manufacturing/` drawings, BOM
